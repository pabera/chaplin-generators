define [
  'chaplin'
], (Chaplin) ->
  'use strict'

  mediator = Chaplin.mediator

  class <%= camelize_name %>Controller extends Chaplin.Controller

    title: '<%= camelize_name %>'
    historyURL: (params) ->
      '<%= plural_object_name %>'

    # initialize: ->
    #   super
    #   @title