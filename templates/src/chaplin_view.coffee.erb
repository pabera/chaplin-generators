define [
  'views/base/view'
  'text!templates/<%= underscore_name %>/<%= @action_name %>.hbs'
], (View, template) ->
  'use strict'

  class <%= camelize_name %><%= Thor::Util.camel_case(@action_name) %>View extends View

    template: template
    template = null
    id: '<%= underscore_name %>_<%= @action_name %>_view'
    container: ''
    autoRender: true

    # initialize: ->
    #   super