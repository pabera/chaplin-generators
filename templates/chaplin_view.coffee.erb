define [
  'views/base/view'
  'text!templates/<%= underscore_name %>/<%= @action_name %>.hbs'
  ], (View, template) ->
   'use strict'

   class <%= camelize_name %>IndexView extends View

    template: template
    template = null
    id: '<%= plural_underscore_name %>_view'
    container: ''
    autoRender: true

    initialize: ->
     super