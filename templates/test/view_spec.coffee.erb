# Spec for <%= camelize_name %>View

define [
  'chaplin'
  'views/<%= underscore_name %>/<%= @action_name %>_view'
], (Chaplin, <%= camelize_name %>View) ->
  'use strict'

  describe 'Views/<%= camelize_name %>/<%= @action_name %>View', ->

    beforeEach ->
      @<%= camelize_name %>View = new <%= camelize_name %>View()

    describe '@initialize', ->

      it 'should have a initialize function', ->
        expect(@<%= camelize_name %>View.initialize).to.be.a('function')