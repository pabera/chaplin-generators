define [
  'chaplin'
  'views/<%= underscore_name %>/index_view'
], (Chaplin, <%= camelize_name %>IndexView) ->
  'use strict'

  mediator = Chaplin.mediator

  class <%= camelize_name %>Controller extends Chaplin.Controller

    title: '<%= camelize_name %>'
    historyURL: (params) ->
      '<%= plural_object_name %>'

    index: (params) ->
      @view = new <%= camelize_name %>IndexView()